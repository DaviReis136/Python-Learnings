#open file and capitalize the text.
try:
    fname = input("Enter file name: ")
    fh = open(fname)
except: 
    print('wrong')
    quit()

for fha in fh:
    fhu = fha.rstrip()
    #print capitalize
    print(fhu.upper()) 
  
